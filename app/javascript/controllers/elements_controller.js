// hello_controller.js
import { Controller } from "stimulus"

export default class extends Controller {
 
 sort(){
 	let element = document.getElementById("elements")
 	let element_items = Array.from(document.getElementByClassName('element-items'))
 }

}